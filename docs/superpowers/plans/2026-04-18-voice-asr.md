# Voice ASR Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 录音结束后调用阿里云 ISI 将音频转文字，识别结果直接发送给 AI，同时保留音频 URL 存档到后端。

**Architecture:** 新建 `src/api/voice.ts` 封装阿里云 Token 获取和 ASR 请求；修改 `InputBar.vue` 的 `handleVoiceFinish` 改为并行上传 + 识别后再发送文字；修改 `useChat.ts` 的 `send()` 使音频附件在有文字时不拼入 WukongIM 消息。

**Tech Stack:** Vue 3, TypeScript, Vite, fetch API (AbortController), 阿里云 NLS REST API (`nls-gateway-cn-shanghai.aliyuncs.com`)

**Spec:** `docs/superpowers/specs/2026-04-18-voice-asr-design.md`

---

## File Map

| 操作 | 路径 | 职责 |
|------|------|------|
| 新建 | `src/api/voice.ts` | `getAliyunToken()`, `transcribeAudio()`, `getAsrFormat()` |
| 修改 | `src/components/chat/InputBar.vue` | 新增 `isTranscribing` 状态、加载 UI、重写 `handleVoiceFinish` |
| 修改 | `src/composables/useChat.ts` | `send()` 内 `audioMarkdown` 当 text 非空时置空 |

---

## Task 1: 新建 `src/api/voice.ts`

**Files:**
- Create: `src/api/voice.ts`

### 实现内容

- `getAsrFormat(mimeType)` — 将 `file.type` 映射为阿里云 format 参数
- `getAliyunToken()` — GET `/eng/voice/aliyunToken` 返回 `{ token, appkey }`
- `transcribeAudio(file, token, appkey, format)` — POST 阿里云 ASR，20s 超时，返回识别文字

- [ ] **Step 1: 新建文件并写入代码**

```typescript
import { http } from '../utils/request'

/** 将 file.type 映射为阿里云 ASR format 参数 */
export function getAsrFormat(mimeType: string): string {
  if (mimeType.includes('webm')) return 'opus'   // Chrome/Edge: audio/webm;codecs=opus
  if (mimeType.includes('mp3'))  return 'mp3'
  if (mimeType.includes('wav'))  return 'wav'
  if (mimeType.includes('aac'))  return 'aac'
  if (mimeType.includes('ogg'))  return 'ogg'
  if (mimeType.includes('opus')) return 'opus'
  return 'mp3'
}

/** 从后端获取阿里云 Token + Appkey */
export async function getAliyunToken(): Promise<{ token: string; appkey: string }> {
  const res = await http.get<{ token: string; appkey: string }>('/eng/voice/aliyunToken')
  return res.data
}

const ASR_ENDPOINT = 'https://nls-gateway-cn-shanghai.aliyuncs.com/stream/v1/asr'
const ASR_TIMEOUT_MS = 20_000

/**
 * 将音频文件发送至阿里云 ISI 一句话识别，返回识别文字。
 * 识别失败时抛出 Error，识别为空时返回空字符串（由调用方处理）。
 */
export async function transcribeAudio(
  file: File,
  token: string,
  appkey: string,
  format: string,
): Promise<string> {
  const url = `${ASR_ENDPOINT}?appkey=${encodeURIComponent(appkey)}&format=${format}&sample_rate=16000&enable_punctuation_prediction=true`

  const controller = new AbortController()
  const timer = setTimeout(() => controller.abort(), ASR_TIMEOUT_MS)

  let res: Response
  try {
    res = await fetch(url, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/octet-stream',
        'X-NLS-Token': token,
      },
      body: file,
      signal: controller.signal,
    })
  } catch (err: unknown) {
    if (err instanceof Error && err.name === 'AbortError') {
      throw new Error('语音识别超时，请重试')
    }
    throw err
  } finally {
    clearTimeout(timer)
  }

  if (!res.ok) {
    throw new Error(`语音识别请求失败（HTTP ${res.status}）`)
  }

  const data = await res.json() as { status: number; result: string; message: string }
  if (data.status !== 20000000) {
    throw new Error(data.message || '语音识别失败')
  }

  return data.result ?? ''
}
```

- [ ] **Step 2: 验证 TypeScript 无报错**

在项目根目录运行：
```bash
npx tsc --noEmit
```
预期：无 error 输出（warnings 可忽略）。

- [ ] **Step 3: 手动验证 `getAsrFormat` 映射正确**

打开浏览器控制台，粘贴：
```js
// 临时测试，验证后删除
import('/src/api/voice.ts').then(m => {
  console.assert(m.getAsrFormat('audio/webm;codecs=opus') === 'opus', 'webm→opus')
  console.assert(m.getAsrFormat('audio/webm') === 'opus', 'webm→opus')
  console.assert(m.getAsrFormat('audio/mp3') === 'mp3', 'mp3→mp3')
  console.assert(m.getAsrFormat('audio/wav') === 'wav', 'wav→wav')
  console.assert(m.getAsrFormat('audio/unknown') === 'mp3', 'fallback→mp3')
  console.log('getAsrFormat: all assertions passed')
})
```
预期：控制台输出 `getAsrFormat: all assertions passed`。

- [ ] **Step 4: Commit**

```bash
git add src/api/voice.ts
git commit -m "feat: add voice ASR API helpers (getAliyunToken, transcribeAudio)"
```

---

## Task 2: 修改 `useChat.ts` — 音频 markdown 在 text 非空时跳过

**Files:**
- Modify: `src/composables/useChat.ts`（`send()` 函数内 `audioMarkdown` 赋值处）

当前代码（约第 311 行）：
```typescript
const audioMarkdown = attachments
  .filter(a => a.mimeType.startsWith('audio/'))
  .map(a => `[🎵 语音消息，URL: ${a.data}](${a.data})`)
  .join('\n')
```

- [ ] **Step 1: 修改 `audioMarkdown` 赋值**

将上述代码替换为：
```typescript
// 当 text 非空时（即语音转文字路径），音频 URL 不发给 AI
// 音频已通过 chatRecordFileList 存档到后端
const audioMarkdown = text
  ? ''
  : attachments
      .filter(a => a.mimeType.startsWith('audio/'))
      .map(a => `[🎵 语音消息，URL: ${a.data}](${a.data})`)
      .join('\n')
```

- [ ] **Step 2: 验证 TypeScript 无报错**

```bash
npx tsc --noEmit
```
预期：无 error。

- [ ] **Step 3: 手动回归测试（图片附件不受影响）**

在 dev server 中发一条带图片附件的消息，确认 WukongIM 仍然收到图片 markdown（`![...](...)`）。图片不受影响是因为 `imageMarkdown` 赋值逻辑未改动。

- [ ] **Step 4: Commit**

```bash
git add src/composables/useChat.ts
git commit -m "fix: skip audio markdown in WukongIM message when text is present (ASR path)"
```

---

## Task 3: 修改 `InputBar.vue` — 加入 ASR 流程和加载状态

**Files:**
- Modify: `src/components/chat/InputBar.vue`

### 3a: 新增状态和导入

- [ ] **Step 1: 在 `<script setup>` 顶部新增导入**

在现有 `import { uploadAudio, uploadFile } from '../../utils/upload'` 之后添加：
```typescript
import { getAliyunToken, transcribeAudio, getAsrFormat } from '../../api/voice'
```

- [ ] **Step 2: 在现有 `const isRecording = ref(false)` 之后添加**

```typescript
const isTranscribing = ref(false)
```

### 3b: 更新模板——增加第三个状态分支

- [ ] **Step 3: 修改模板中的 `v-if="isRecording"` 区块**

将原来的：
```html
<div v-if="isRecording" class="transition-all">
  <VoiceRecorder @cancel="isRecording = false" @finish="handleVoiceFinish" />
</div>

<div v-else :class="[...]" ...>
  <!-- 输入框 -->
</div>
```

改为（中间插入 `v-else-if`）：
```html
<div v-if="isRecording" class="transition-all">
  <VoiceRecorder @cancel="isRecording = false" @finish="handleVoiceFinish" />
</div>

<div v-else-if="isTranscribing"
  class="flex items-center gap-3 px-4 bg-blue-50/50 border border-blue-200 rounded-xl h-[56px] shadow-inner mb-2">
  <svg class="w-4 h-4 text-blue-500 animate-spin" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
    <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
    <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8v8z"></path>
  </svg>
  <span class="text-sm text-blue-700">正在识别语音...</span>
</div>

<div v-else :class="[...]" ...>
  <!-- 输入框（原有代码不变） -->
</div>
```

### 3c: 重写 `handleVoiceFinish`

- [ ] **Step 4: 替换 `handleVoiceFinish` 函数**

将现有：
```typescript
async function handleVoiceFinish(file: File) {
  isRecording.value = false
  try {
    const url = await uploadAudio(file)
    const voiceAtt: Attachment = {
      name: file.name,
      mimeType: file.type,
      data: url
    }
    // 直接单发语音消息
    await chat.send('', [voiceAtt])
  } catch (err) {
    toast.value = err instanceof Error ? err.message : '语音上传失败'
  }
}
```

替换为：
```typescript
async function handleVoiceFinish(file: File) {
  if (isTranscribing.value) return  // 防重入
  isRecording.value = false
  isTranscribing.value = true
  toast.value = ''

  // 最小有效录音：< 1KB 视为意外触发
  if (file.size < 1024) {
    toast.value = '录音时间太短，请重试'
    isTranscribing.value = false
    return
  }

  try {
    const format = getAsrFormat(file.type)
    // 并行：上传音频 + 取Token后立即 ASR
    const [audioUrl, asrText] = await Promise.all([
      uploadAudio(file),
      getAliyunToken().then(({ token, appkey }) =>
        transcribeAudio(file, token, appkey, format)
      ),
    ])

    if (!asrText.trim()) {
      toast.value = '未能识别语音内容，请重试'
      return
    }

    await chat.send(asrText, [{
      name: file.name,
      mimeType: file.type,
      data: audioUrl,
    }])
  } catch (err) {
    toast.value = err instanceof Error ? err.message : '语音识别失败，请重试'
  } finally {
    isTranscribing.value = false
  }
}
```

- [ ] **Step 5: 验证 TypeScript 无报错**

```bash
npx tsc --noEmit
```
预期：无 error。

- [ ] **Step 6: 本地功能验证**

启动 dev server（`npm run dev` 或 `bun run dev`），按以下步骤验证：

1. 点击麦克风按钮 → 显示 VoiceRecorder 录音 UI
2. 录音 2-3 秒后点击"发送"
3. VoiceRecorder 消失 → 出现"正在识别语音..."加载动画
4. 识别完成 → 加载动画消失 → 消息列表出现一条用户文字消息
5. 打开 Network 面板确认：
   - `POST /eng/agent/addChatRecordData` 请求体包含 `chatRecordFileList`（音频 URL）
   - `POST nls-gateway-cn-shanghai.aliyuncs.com/stream/v1/asr` 返回 200 和 `status: 20000000`
6. AI 收到的消息是纯文字（不含音频 URL markdown）

- [ ] **Step 7: 验证 CORS**

在 Network 面板查看阿里云 ASR 请求：
- 若请求正常返回 → CORS 已开放，功能完整
- 若出现 CORS 错误 → 需要后端添加代理接口 `POST /eng/voice/transcribe`（见附录）

- [ ] **Step 8: Commit**

```bash
git add src/components/chat/InputBar.vue
git commit -m "feat: voice-to-text via Aliyun ISI — transcribe before sending to AI"
```

---

## Task 4: 边界情况手动测试

- [ ] **Step 1: 测试取消录音**

点击麦克风 → 录音中点击"取消" → 确认无任何 toast 出现，输入框正常显示。

- [ ] **Step 2: 测试极短录音（< 1KB）**

快速点击麦克风然后立即点击"发送" → 应出现 toast"录音时间太短，请重试"。

- [ ] **Step 3: 测试 ASR 空结果（若能模拟）**

在安静环境下录制一段几乎无声的录音发送 → 应出现 toast"未能识别语音内容，请重试"，不发送消息。

- [ ] **Step 4: 测试历史消息加载**

发送语音消息后刷新页面，切换到该会话 → 确认消息列表显示转换后的文字内容，不显示音频 URL markdown。

---

## 附录：CORS 受限时的后端代理方案

若 Task 3 Step 7 发现 CORS 错误，需后端新增代理接口，前端 `transcribeAudio` 函数改为调用后端：

**后端接口（由后端实现）：**
```
POST /eng/voice/transcribe?format=<format>
Content-Type: application/octet-stream
Body: 音频二进制数据
Response: { "result": "识别文字" }
```

**前端 `transcribeAudio` 改为：**
```typescript
export async function transcribeAudio(
  file: File,
  _token: string,
  _appkey: string,
  format: string,
): Promise<string> {
  const controller = new AbortController()
  const timer = setTimeout(() => controller.abort(), ASR_TIMEOUT_MS)

  let res: Response
  try {
    res = await fetch(`/eng/voice/transcribe?format=${format}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/octet-stream' },
      body: file,
      signal: controller.signal,
    })
  } catch (err: unknown) {
    if (err instanceof Error && err.name === 'AbortError') {
      throw new Error('语音识别超时，请重试')
    }
    throw err
  } finally {
    clearTimeout(timer)
  }

  if (!res.ok) throw new Error(`语音识别请求失败（HTTP ${res.status}）`)
  const data = await res.json() as { result: string }
  return data.result ?? ''
}
```

此时 `getAliyunToken()` 调用可移至后端，前端无需传 token/appkey 参数。
